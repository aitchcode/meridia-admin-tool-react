import colors from "core/constants/color";

const baseTheme = {
  palette: {
    primary: {
      main: colors.indigo,
    },
    secondary: {
      main: colors.grey,
    },
  },
};

export default baseTheme;
